  Thanks for submitting the form!  Here are your answers:<br>
  Name: <%= @answer.name %><br>
  <% @answer.attributes.each do |attr| %>
    <% unless ["id", "name", "created_at", "updated_at"].include?(attr[0])  %>
      <%= render partial: 'item', locals: { item: attr } if attr[0] %>
    <% end %>
  <% end %>
